Task ID:
ENT-004

Model:
haiku

Title:
Create Actions/SummonRelicAction.cs

Purpose:
The project's new "Relic" board entity is summoned from hand into the SAME `Player.Board` list a `Minion` uses (this is what makes board-slot capacity and left-to-right adjacency both correct automatically — a Relic occupies a real position in the shared sequence, so a Relic between two Minions genuinely breaks their adjacency for effects like Cleave, with no extra bookkeeping). This task creates the action that inserts a `Relic` into `Player.Board`, mirroring `CardBattleEngine\Actions\SummonMinionAction.cs` almost exactly — same `PlayIndex`/`IndexOffset` insertion logic, same board-full check against `GameState.MaxBoardSize` (shared with Minions, no separate cap).

Dependencies:
PHASEA-EXIT (this task is part of the ENT phase and must not start until the FOUND phase checkpoint passes)

Consumes:
- `Relic` class (produced by ENT-002, running in parallel) — trust constructor `Relic(RelicCard card, Player owner)` exists.
- `RelicCard` class (produced by ENT-003, running in parallel).
- `ActionContext.SummonedRelic` field (produced by ENT-007, running in parallel).
- `Player.Board` (`List<BoardPermanent>`, produced by ENT-001 + WIRE-002, running in parallel) — trust it is a mutable list that Minions and Relics both live in, and that `GameState.MaxBoardSize` (already exists, unchanged) is the single shared cap for both.

Produces:
- `SummonRelicAction` class — consumed by ENT-003 (RelicCard.cs), EXIT-001.

Files:
CardBattleEngine\Actions\SummonRelicAction.cs (new file)

Requirements:
Create the file with exactly this content (direct mirror of `SummonMinionAction.cs`, inserting a `Relic` into the shared `Board` instead of a `Minion` — no Reborn-related logic, Relics don't have that concept):
```csharp
namespace CardBattleEngine;

public class SummonRelicAction : GameActionBase
{
	public RelicCard Card { get; set; }
	public int IndexOffset { get; set; }
	public override EffectTrigger EffectTrigger => EffectTrigger.None;

	public override bool IsValid(GameState state, ActionContext actionContext, out string reason)
	{
		if (actionContext.SourcePlayer.Board.Count >= state.MaxBoardSize)
		{
			reason = null;
			return false;
		}

		reason = null;
		return true;
	}

	public override IEnumerable<(IGameAction, ActionContext)> Resolve(GameState state, ActionContext actionContext)
	{
		if (!IsValid(state, actionContext, out var _))
			return [];

		var relic = actionContext.SummonedRelic
			?? new Relic(Card, actionContext.SourcePlayer);

		var list = actionContext.SourcePlayer.Board;
		int playIndex = actionContext.PlayIndex;

		int clampedIndex =
			(playIndex == -1 || playIndex < 0 || playIndex > list.Count)
				? list.Count
				: playIndex;
		var offsetIndex = clampedIndex + IndexOffset;

		clampedIndex =
			(offsetIndex == -1 || offsetIndex < 0 || offsetIndex > list.Count)
				? list.Count
				: offsetIndex;

		list.Insert(clampedIndex, relic);

		actionContext.SummonedRelic = relic;

		return [];
	}
}
```

Constraints:
- Create only this one new file. Do not edit `SummonMinionAction.cs` or any other existing file.
- Insert into `actionContext.SourcePlayer.Board` — do NOT create or reference any separate `Relics` list; there isn't one. Relics live only in `Board`, exactly like Minions.
- The capacity check is against the shared `GameState.MaxBoardSize` — do not introduce a separate Relic-only cap.

Acceptance Criteria:
- File exists at the exact path above, subclassing `GameActionBase`.
- `IsValid` returns `false` when `player.Board.Count >= state.MaxBoardSize`.
- `Resolve` inserts the relic into `player.Board` at the same clamped `PlayIndex`/`IndexOffset` position `SummonMinionAction` uses for Minions, and sets `ActionContext.SummonedRelic`, reusing an already-provided `SummonedRelic` if one exists.

Validation:
This file references `Relic`, `RelicCard`, `ActionContext.SummonedRelic`, and `Player.Board` (with its new element type), all created/changed by sibling tasks in this same batch — it cannot build alone in isolation (expected). Confirm the code above is transcribed exactly. Full solution build validated by EXIT-001.

Handoff:
ENT-003 constructs and calls `IsValid` on this action by this exact name and shape.

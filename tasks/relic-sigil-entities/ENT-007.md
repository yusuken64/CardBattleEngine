Task ID:
ENT-007

Model:
haiku

Title:
Add ActionContext.SummonedRelic field to Interfaces/GameActionBase.cs

Purpose:
When a Minion is played from hand, `MinionCard.GetPlayEffects` creates the `Minion` instance up front and stashes it on `ActionContext.SummonedMinion` so `SummonMinionAction` and any Battlecry effects reference the exact same instance (see `MinionCard.cs`/`SummonMinionAction.cs`). The project's new "Relic" card type needs the same hand-off: `RelicCard.GetPlayEffects` creates a `Relic` and stashes it on a new `ActionContext.SummonedRelic` field for `SummonRelicAction` to pick up.

Dependencies:
PHASEA-EXIT (this task is part of the ENT phase, not FOUND, precisely because it forward-references `Relic` — it must not start until the FOUND phase's checkpoint passes)

Consumes:
`Relic` class (produced by ENT-002, running in parallel in this same phase) — this task only needs to know the type name `Relic` exists; trust it will exist by the time the solution builds as a whole.

Produces:
- `ActionContext.SummonedRelic` (`Relic`) field, copied by `ActionContext.ShallowCopy()` — consumed by ENT-003 (RelicCard.cs) and ENT-004 (SummonRelicAction.cs).

Files:
CardBattleEngine\Interfaces\GameActionBase.cs

Requirements:
1. In the `ActionContext` class, add a new field right next to the existing `SummonedMinion` field:
```csharp
public Minion SummonedMinion { get; set; }
public Relic SummonedRelic { get; set; }
```
2. In `ActionContext.ShallowCopy()`, add a copy line right next to the existing `SummonedMinion` copy:
```csharp
newContext.SummonedMinion = this.SummonedMinion;
newContext.SummonedRelic = this.SummonedRelic;
```

Constraints:
- Scope this diff to exactly this one file, exactly these two additions.
- `SummonedMinion` is NOT copied in the `ActionContext(ActionContext context)` copy constructor (only in `ShallowCopy()`) — match that exact behavior; do not add `SummonedRelic` there either.

Acceptance Criteria:
- `ActionContext.SummonedRelic` property exists, type `Relic`. `ShallowCopy()` copies it. No other field's copying behavior changed.

Validation:
This file alone cannot make the full solution build (it references `Relic`, created by ENT-002, a separate task in this batch — expected). Full solution integration validated by EXIT-001.

Handoff:
ENT-003 and ENT-004 both read/write `ActionContext.SummonedRelic` by this exact name.

Task ID:
FWD-001

Model:
haiku

Title:
Migrate Target -> Targets in Actions\CastSpellAction.cs (mechanical rename only)

Purpose:
Part of the solution-wide mechanical rename from `ActionContext.Target` (singular) to `ActionContext.Targets` (list). This file will later receive additional functional changes in Phase B (task MTC-007, a new passthrough branch for multi-target spells) — this task must do ONLY the mechanical rename, not that future work, to keep the diffs separable.

Dependencies:
CTX-000

Consumes:
CTX-000: `ActionContext.Targets` field shape (`List<IGameEntity>`)

Produces:
Mechanically-renamed `CastSpellAction.cs`, which task MTC-007 (Phase B) will later add a new branch to.

Files:
CardBattleEngine\Actions\CastSpellAction.cs

Requirements:
Apply these substitution rules to every occurrence of `.Target` (singular) in this file, including the `new ActionContext { ... }` construction sites inside the per-target fan-out loop (e.g. `Target = target` becoming `Targets = [target]`) and any `Target = null` sentinel used when passing an `AffectedEntitySelector` through without a resolved target:

| Old code pattern | New code pattern |
|---|---|
| `x.Target != null` | `x.Targets is { Count: > 0 }` |
| `x.Target` (read as a value) | `x.Targets?.FirstOrDefault()` |
| `Target = <expr>` in an object initializer, `<expr>` provably non-null | `Targets = [<expr>]` |
| `Target = null` | `Targets = null` |

Do NOT use `.Single()` anywhere. Do NOT add any new branch, condition, or logic — specifically, do not attempt to add a "pass Targets through without fan-out" code path here; that is explicitly out of scope for this task and is handled later by a separate task (MTC-007) once Phase A is fully complete and stable.

Constraints:
- Scope this diff to exactly this one file.
- Pure rename only — no new branches, no new logic, no reordering of existing branches.

Acceptance Criteria:
- No bare `.Target` (singular) reference remains anywhere in this file.
- Every substitution follows the table above with correct null/list semantics.
- The existing branch structure (selector-null vs `Once` vs `PerAction` resolution timing) is unchanged in shape — only the field name changed.

Validation:
This file alone cannot make the full solution build (many sibling files still reference the old field until their own tasks land) — that's expected. Confirm via search that no bare singular `.Target` reference remains in this file. Full solution build and test-suite pass is validated by the PHASEA-EXIT task once all sibling tasks land.

Handoff:
Task MTC-007 (Phase B) depends on this task landing first and touches this same file — sequence MTC-007 after PHASEA-EXIT to avoid merge conflicts.

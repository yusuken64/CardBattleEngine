Task ID:
FWD-009

Model:
haiku

Title:
Migrate Target -> Targets in Engine\GameState.cs (mechanical rename only — existing single-target cross-product loop)

Purpose:
Part of the solution-wide mechanical rename from `ActionContext.Target` (singular) to `ActionContext.Targets` (list). This file will later receive a real new branch in Phase B (task MTC-004: emitting a "begin multi-target selection" action instead of flattening the cross-product for multi-target cards) — this task must do ONLY the mechanical rename of the EXISTING single-target loop, not add that new branch.

Dependencies:
CTX-000

Consumes:
CTX-000: `ActionContext.Targets` field shape (`List<IGameEntity>`)

Produces:
Mechanically-renamed `GameState.cs`, which task MTC-004 (Phase B) will later add a new "begin multi-target selection" branch to, inside `GetValidActions`.

Files:
CardBattleEngine\Engine\GameState.cs

Requirements:
Inside `GetValidActions`, apply these specific translations to the existing single-target cross-product loop, preserving behavior exactly:
- `Target = target` (per-candidate-target loop) -> `Targets = [target]`
- `Target = OpponentOf(player)` (attack-enumeration branch) -> `Targets = [OpponentOf(player)]`
- `Target = defender` (attack-enumeration branch) -> `Targets = [defender]`
- `Target = actionContext.SourcePlayer` (weapon-card branch) -> `Targets = [actionContext.SourcePlayer]`

Apply the standard mechanical substitution rules to any other `.Target` usages elsewhere in this file:

| Old code pattern | New code pattern |
|---|---|
| `x.Target != null` | `x.Targets is { Count: > 0 }` |
| `x.Target` (read as a value) | `x.Targets?.FirstOrDefault()` |
| `Target = <expr>` in an object initializer, `<expr>` provably non-null | `Targets = [<expr>]` |
| `Target = null` | `Targets = null` |

Do NOT use `.Single()` anywhere.

Constraints:
- Scope this diff to exactly this one file.
- Pure rename only — do not add any new branch for multi-target cards, do not reference `Card.RequiredTargetCount` (it doesn't exist yet at this point in the plan). That new branch is explicitly deferred to task MTC-004 in Phase B.
- Do not change the structure, order, or count of actions produced by `GetValidActions` for any existing card.

Acceptance Criteria:
- No bare `.Target` (singular) reference remains anywhere in this file.
- `GetValidActions`'s output (the set of `(IGameAction, ActionContext)` pairs it produces) is identical before and after this change for every existing card and every existing test scenario.

Validation:
This file alone cannot make the full solution build (many sibling files still reference the old field until their own tasks land) — that's expected. Confirm via search that no bare singular `.Target` reference remains in this file. Full solution build and test-suite pass (including `ValidActionTest.cs`, migrated separately in MECH-037) is validated by the PHASEA-EXIT task once all sibling tasks land.

Handoff:
Task MTC-004 (Phase B) depends on this task landing first and touches this same file's `GetValidActions` method — sequence MTC-004 after PHASEA-EXIT to avoid merge conflicts.

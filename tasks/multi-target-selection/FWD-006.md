Task ID:
FWD-006

Model:
haiku

Title:
Migrate Target -> Targets in Actions\DeathAction.cs (mechanical rename only)

Purpose:
Part of the solution-wide mechanical rename from `ActionContext.Target` (singular) to `ActionContext.Targets` (list). This file is a dependency for the Phase C proof-of-concept (`SwapHealthAction`, task POC-001), which constructs a `DeathAction` context using the new `Targets` shape.

Dependencies:
CTX-000

Consumes:
CTX-000: `ActionContext.Targets` field shape (`List<IGameEntity>`)

Produces:
Mechanically-renamed `DeathAction.cs`, consumed by task POC-001 (Phase C, `SwapHealthAction` constructs `new ActionContext { ..., Targets = [entity] }` when yielding a `DeathAction`).

Files:
CardBattleEngine\Actions\DeathAction.cs

Requirements:
Apply these substitution rules to every occurrence of `.Target` (singular) in this file. Specifically:
- `actionContext.Target.IsAlive` (or similar direct property access) becomes a guarded read off the first chosen target, e.g. `actionContext.Targets?.FirstOrDefault()?.IsAlive == true` (adapt the exact null-handling to match the surrounding code's existing style).
- `Target = target` in the Deathrattle fan-out block becomes `Targets = [target]`.

| Old code pattern | New code pattern |
|---|---|
| `x.Target != null` | `x.Targets is { Count: > 0 }` |
| `x.Target` (read as a value) | `x.Targets?.FirstOrDefault()` |
| `Target = <expr>` in an object initializer, `<expr>` provably non-null | `Targets = [<expr>]` |
| `Target = null` | `Targets = null` |

Do NOT use `.Single()` anywhere. Do not change any logic beyond this substitution — this is a pure rename, no behavior change.

Constraints:
- Scope this diff to exactly this one file.
- Pure rename only.

Acceptance Criteria:
- No bare `.Target` (singular) reference remains anywhere in this file.
- Every substitution follows the table above with correct null/list semantics.
- Deathrattle fan-out and death-check behavior is unchanged for any existing single-target scenario.

Validation:
This file alone cannot make the full solution build (many sibling files still reference the old field until their own tasks land) — that's expected. Confirm via search that no bare singular `.Target` reference remains in this file. Full solution build and test-suite pass is validated by the PHASEA-EXIT task once all sibling tasks land.

Handoff:
Task POC-001 (Phase C) depends on this task having landed (it constructs `ActionContext` instances targeting `DeathAction` using the new `Targets` shape).

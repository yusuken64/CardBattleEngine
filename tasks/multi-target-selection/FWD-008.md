Task ID:
FWD-008

Model:
haiku

Title:
Migrate Target -> Targets in Actions\PlayCardAction.cs (mechanical rename only — stealth/elusive check)

Purpose:
Part of the solution-wide mechanical rename from `ActionContext.Target` (singular) to `ActionContext.Targets` (list). This file will later receive real functional changes in Phase B (task MTC-005: full target-list validation and multi-target stealth/elusive checking) — this task must do ONLY the mechanical, behavior-preserving rename, not that future work.

Dependencies:
CTX-000

Consumes:
CTX-000: `ActionContext.Targets` field shape (`List<IGameEntity>`)

Produces:
Mechanically-renamed `PlayCardAction.cs`, which task MTC-005 (Phase B) will later upgrade with real multi-target validation logic.

Files:
CardBattleEngine\Actions\PlayCardAction.cs

Requirements:
In `IsValid`, apply these specific translations, preserving single-target behavior exactly:
1. The stealth/elusive type-check, e.g. `actionContext.Target is Minion minion`, becomes `actionContext.Targets?.FirstOrDefault() is Minion minion`.
2. The target-membership check, e.g. `validTargets.Contains(actionContext.Target)`, becomes `validTargets.Contains(actionContext.Targets?.FirstOrDefault())`. **Do not** upgrade this to real list-length/multi-target validation in this task — keep it checking only the first entry, exactly matching current single-target behavior. That upgrade is explicitly deferred to task MTC-005 in Phase B.

Apply the standard mechanical substitution rules to any other `.Target` usages elsewhere in this file:

| Old code pattern | New code pattern |
|---|---|
| `x.Target != null` | `x.Targets is { Count: > 0 }` |
| `x.Target` (read as a value) | `x.Targets?.FirstOrDefault()` |
| `Target = <expr>` in an object initializer, `<expr>` provably non-null | `Targets = [<expr>]` |
| `Target = null` | `Targets = null` |

Do NOT use `.Single()` anywhere.

Constraints:
- Scope this diff to exactly this one file.
- Pure rename only — do not add `RequiredTargetCount` checks, do not validate list length, do not check stealth/elusive against more than the first target. All of that is explicitly deferred to MTC-005.

Acceptance Criteria:
- No bare `.Target` (singular) reference remains anywhere in this file.
- `IsValid`'s behavior for any existing single-target card is identical before and after this change.

Validation:
This file alone cannot make the full solution build (many sibling files still reference the old field until their own tasks land) — that's expected. Confirm via search that no bare singular `.Target` reference remains in this file. Full solution build and test-suite pass is validated by the PHASEA-EXIT task once all sibling tasks land.

Handoff:
Task MTC-005 (Phase B) depends on this task landing first and touches this same file's `IsValid` method — sequence MTC-005 after PHASEA-EXIT to avoid merge conflicts.

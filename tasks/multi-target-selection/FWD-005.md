Task ID:
FWD-005

Model:
haiku

Title:
Migrate Target -> Targets in Entities\MinionCard.cs battlecry fan-out block (mechanical rename only)

Purpose:
Part of the solution-wide mechanical rename from `ActionContext.Target` (singular) to `ActionContext.Targets` (list). This task is mechanical rename only in Phase A; task MTC-001 (Phase B) later adds the `RequiredTargetCount` property propagation to this same file's `Clone()` method — kept separate to avoid mixing concerns in one diff.

Dependencies:
CTX-000

Consumes:
CTX-000: `ActionContext.Targets` field shape (`List<IGameEntity>`)

Produces:
Mechanically-renamed `MinionCard.cs`, which task MTC-001 (Phase B) will later add one property-propagation line to in its `Clone()` method.

Files:
CardBattleEngine\Entities\MinionCard.cs

Requirements:
In the Battlecry fan-out block, apply these substitution rules to every occurrence of `.Target` (singular). Specifically, a line like `targets = [context.Target];` becomes `targets = context.Targets ?? [];`.

| Old code pattern | New code pattern |
|---|---|
| `x.Target != null` | `x.Targets is { Count: > 0 }` |
| `x.Target` (read as a value) | `x.Targets?.FirstOrDefault()` |
| `Target = <expr>` in an object initializer, `<expr>` provably non-null | `Targets = [<expr>]` |
| `Target = null` | `Targets = null` |

Do NOT use `.Single()` anywhere. Do not change any logic beyond this substitution — this is a pure rename, no behavior change. Do not touch `Clone()` or add any `RequiredTargetCount`-related code — that is explicitly out of scope for this task (handled later by MTC-001).

Constraints:
- Scope this diff to exactly this one file.
- Pure rename only in the battlecry fan-out block; leave `Clone()` completely untouched.

Acceptance Criteria:
- No bare `.Target` (singular) reference remains anywhere in this file.
- Every substitution follows the table above with correct null/list semantics.
- `Clone()` is byte-for-byte unchanged by this task.

Validation:
This file alone cannot make the full solution build (many sibling files still reference the old field until their own tasks land) — that's expected. Confirm via search that no bare singular `.Target` reference remains in this file. Full solution build and test-suite pass is validated by the PHASEA-EXIT task once all sibling tasks land.

Handoff:
Task MTC-001 (Phase B) depends on this task landing first and touches this same file's `Clone()` method — sequence MTC-001 after PHASEA-EXIT to avoid merge conflicts.
